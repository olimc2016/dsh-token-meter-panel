# 变更记录

## 0.6.1 — 首屏不再被超大会话日志拖死

### 修复

- **工作电脑首次打开要 70 多秒**：`aggregate()` 全量解压扫描所有会话日志，日志大的机器会把首屏拖到一分钟以上。
  现在加**硬时间预算**（默认 8 秒，`scanBudgetMs` 可调）：超预算立即停手，
  **用已扫到的（最新优先）部分出数**，结果里标 `partialScan`
- 文件按 mtime 倒序扫描，超预算时保住的是"最近/今天"这部分，最影响观感的数据不会缺
- 会话日志特别大的机器可把 `scanBudgetMs` 调小（例如 3000）

## 0.6.0 — 后台静默更新

### 新增

- **发现新版本后自动在后台升级**：无需任何点击/复制粘贴，升级完成后**下次重启 DSH 自动生效**
  - 面板底部显示状态：`有新版本 vX.XX · 后台自动更新中（重启 DSH 生效）` → `已后台更新完成，重启 DSH 生效`
  - 失败时显示 `后台更新失败（缺 pnpm？）`，并保留「复制更新命令」作为手动兜底
- **安全边界（重要）**：
  - 执行的命令是**写死的白名单形式**：`dsh plugin --profile <profile> add github:olimc2016/dsh-token-meter-panel#v<版本>`
    （版本号先做 `^\d+\.\d+\.\d+$` 校验，杜绝命令注入；不接受任何外部输入拼接）
  - **不在启动阶段执行**：只在插件运行满 60 秒后、由「发现新版本」触发，且每个版本只做一次（吸取 0.5.2 启动超时的教训）
  - 可在设置里关闭：**设置 → 插件 → 后台静默更新**（关掉就只提示、不自动升级）
  - 只改你 profile 里的这一个依赖，不碰其它任何东西

## 0.5.4 — 中栏金额改为「峰/谷分开算，再汇总」

### 修复

- **中栏公式原先用「当前时段单价 × 全天 token」一刀切**：跨档的日子（凌晨空闲 + 上午高峰）
  算出来和官方金额对不上。现在聚合层按 `isPeakHour()` **把 token 分成峰/谷两组分别计价**，
  中栏显示三行：
  ```
  峰 ¥0.72 = 命中 13.1M × ¥0.04 + 未命中 12K × ¥2 + 输出 22K × ¥8
  谷 ¥1.74 = 命中 51.6M × ¥0.02 + 未命中 89K × ¥1 + 输出 155K × ¥4
  合计 ¥2.46（官方；峰/谷按本机日志分档）
  ```
- 实测闭合：两档 token 相加 = 全天 token，金额差 **0.000000**
- 官方接口不给「分档」这个维度，所以峰/谷的 token 与金额来自本机日志（面板上已标注），
  **合计仍以官方金额为准**
- 没有分档数据时（当天无本地日志）退回单行展示，并标注「按当前时段单价」

## 0.5.3 — 官方数据真正落到面板上

### 修复

- **面板一直停在「正在获取官方数据…」**：0.4.1 把官方取数改成「后台补齐 + 客户端轮询」，
  客户端每次拿到的都是缓存里的旧响应，时序对不上 —— 宿主日志明明已经
  `official usage: ok in 325ms`，面板却永远 loading。**改为在同一个 `/summary` 响应里直接带回官方数据**
  （实测 325ms，等得起），彻底消除缓存时序问题
- 取数并发去重（`inflight`）：客户端 1.5 秒重试时不再堆叠出多次取数
- 25 秒硬超时护栏 + 起止日志：任何情况都有明确终态，不会再"既不成功也不失败"地挂着

### 界面

- 「更新于 HH:MM:SS · 账户余额 ¥x」和 **⟳ 刷新** 按钮一起挪进 **今日消费**卡（顶部标题行只留标题）

## 0.5.2 — 紧急修复：启动预热导致 DSH 进入恢复模式

### 修复（严重）

- **根因**：0.4.1 加的「启动预热」`setTimeout(() => summary(), 0)`。`summary()` 里的
  `aggregate()` 是**同步 CPU 重活**（解压 + 扫描全部会话日志），在 DSH 启动窗口里触发会堵住事件循环，
  让「启动插件服务」这一步超时 → **DSH 进入恢复模式**（界面提示 `DSH Host call cancelled or timed out`）
- **修法**：**彻底移除启动预热**。首屏速度改由客户端负责：先渲染 + 读秒，再等 `/summary` 返回，
  必要时 1.2 秒快速重试（这是本来就有的机制，不需要宿主在启动阶段抢跑）
- 教训：插件的 `apply()` 里不能启动任何重活（CPU 密集或网络），启动阶段只做注册

## 0.5.1 — 紧急修复：装 0.5.0 会导致 DSH 起不来

### 修复（严重）

- **根因**：`src/chrome-storage.mjs` 顶层写了 `import { zstdDecompressSync } from 'node:zlib'`。
  这个导出只在 Node ≥ 22.15 存在，**老版本 Node 上命名导入会在模块链接期直接抛错** →
  `lib/index.js` 导入失败 → 插件加载失败 → DSH Desktop 启动被带崩
- **修法（两道防线）**：
  1. 改为 `import zlib from 'node:zlib'`，用到时再判断 `typeof zlib.zstdDecompressSync === 'function'`
  2. `lib/index.js` 改成**动态导入**读取器（`await import(...)`），
     读取器即便加载失败也只是降级成「未连接」，**绝不可能再拖垮插件与 DSH 启动**
- 教训：插件里任何「宿主 Node 版本相关」的 API 都不能用顶层命名导入，一律延迟取用 + 能力判断

## 0.5.0 — 版本号、开源协议与更新提示

### 新增

- **首次加载读秒**：首屏显示转圈 + `首次加载，请稍等 Ns`（每秒递增）+ 小字说明
  「要读浏览器登录态并扫描本机会话日志，通常几秒」——用户知道在动，不会以为卡死
- **全程自动，无需用户操作**：官方数据的获取顺序是「凭据 → 浏览器登录态」全自动；官方数据后台补齐
  （不阻塞首屏），客户端每 1.2 秒快速重试至多 8 次。「连接官方」**只在真的读不到时**才出现，是兜底而非必经步骤
- **面板底部显示版本号与开源协议**：`dsh-token-meter-panel v0.5.0 · MIT 开源协议`
  （协议名可点击，直达仓库里的 `LICENSE`）
- **更新提示**：宿主每 6 小时查一次 GitHub Releases（只查，不下载、不安装；失败静默）
  - 有新版本时底部出现：`有新版本 v0.5.1` + **复制更新命令**（一键把升级命令复制到剪贴板，粘进终端即可）
    + **发布说明 ↗**（打开该版本的 Release 页）
  - 为什么不自动安装：升级要改你的 profile（依赖 pnpm）并且**必须重启 DSH 才生效**，
    静默替用户执行命令 + 改 profile 风险太大，所以做成"一键复制命令"，由你确认执行
  - 查询走公开接口 `api.github.com/repos/olimc2016/dsh-token-meter-panel/releases/latest`，
    不发送任何凭据、不采集使用数据

## 0.4.1 — 首屏秒开

### 修复

- **首次打开面板太慢**：首个 `/summary` 原先要同步等「读浏览器登录态 + 3 次官方 HTTP + 扫全部会话日志」才返回。
  现在改成**不阻塞首屏**：
  - 有官方缓存就直接用；没有就先标 `正在获取官方数据…`，后台补齐
  - 客户端在等官方数据时**每 1.2 秒自动重试**（最多 8 次），不用干等到下一个 60 秒轮询
  - 插件**激活时预热**一次（用户点开面板前通常已经算好，直接命中缓存）

## 0.4.0 — 点击即用：零配置连上官方用量

### 新增

- **自动读取浏览器登录态**：宿主侧内置一个**纯 JS（零依赖、无原生模块）**的 Chromium Local Storage 读取器
  （自实现 Snappy 解压 + LevelDB 只读 + WAL），直接取 `platform.deepseek.com` 的 `userToken`。
  **用户不需要复制粘贴、不需要 DevTools、不需要编辑凭据文件** —— 装好打开面板，官方数据自己就来了
- **连不上的兜底**：没有登录态时，面板显示「官方：未连接 **[连接官方]** 首次会打开官方网页，未登录请先在页面里登录，回来即自动生效」；
  点一下打开官方账单页，登录后回到面板即自动生效（凭据名 `DEEPSEEK_PLATFORM_TOKEN` 仍然优先，可手动指定）
- 连接成功后那一行会标注 **· 浏览器登录态**，一眼看出数据是怎么来的

### 实现说明

- 支持 Edge / Chrome / Chromium / Brave / Vivaldi / Yandex（Windows 含 Beta/Dev 通道，macOS/Linux 路径同写）；
  `CHROMIUM_USER_DATA_DIRS` 可追加自定义 User Data 目录
- 只读流程：整目录复制到系统临时目录再解析（浏览器占用原文件），读完即删，**不写用户目录**
- **只取 `platform.deepseek.com` 这一条记录**，不外发、不落盘
- 读不到的情况：站点未登录、浏览器把数据放在非标准 profile、文件被独占、块损坏、超过 20 秒限时、
  或 Chromium 未来改存储格式 —— 这些都会自动退回到「未连接 + 手动连接」提示，不会变成死路

## 0.3.0 — 皮肤框架与液态玻璃

### 新增

- **皮肤（skin）设置项**：`default`（跟随 DSH 主题）/ `glass`（液态玻璃），
  在**设置 → 插件 → dsh-token-meter-panel** 里切换，改完即时生效
- **液态玻璃**：面板与卡片改为半透明 + `backdrop-filter: blur(22px) saturate(170%)`，
  加 1px 内高光边、更大圆角、柔和投影；首页「今日消费」卡叠一层品牌色渐变

### 设计约束（后面加皮肤都照这个来）

- 皮肤 = **一组「表面 token」**（背景/边框/圆角/模糊/投影），几何与信息层级不动
- 颜色一律写成**基于 `--dsw-*` 主题变量的变换**（`color-mix`），不写死 hex →
  深浅主题切换时皮肤自动跟随；DSH 改主题也不会把皮肤弄崩
- **文字与语义色不参与皮肤化**：金额、命中/未命中/输出三色在所有皮肤下保持一致，
  保证金额始终清晰、三色在色盲模拟下仍可区分
- 浏览器不支持 `backdrop-filter` 时自动退化为半透明卡片（不依赖模糊也能看）

## 0.2.0 — 把「省了多少钱」放到台前

### 面板

- **官方口径为主**：能拿到官方用量时，今日消费的**大数字直接用官方数**（带「官方」徽标），
  下面一行给「本地日志 ¥Y · 差 ¥Z · 调用 N（官方 M）」；官方不可用时退回本地日志并标明来源。
  官方口径的环比用「较昨天」（同一接口顺带取昨天的官方数）
- **对账结论（2026-09-20 实测）**：逐小时比对官方 Platform 用量接口与本地日志，
  **同一 API Key 覆盖的时段内完全一致**（命中 token 比值 = 1.00，请求数 123/123、166/157）。
  本地曾比官方多约 45%，根因是当天 13:51 换过 API Key，上午那批调用记在旧 Key 上，当前账户看不到

- **接入官方用量**：向 DeepSeek Platform 的用量端点查询当日消费，面板并列显示
  「官方 ¥X（platform.deepseek.com/api/v0/usage/by_api_key）· 本地推算 ¥Y · 差 ¥Z」；
  官方不可用时显示具体原因（未配置 userToken / 已关闭 / 请求失败）。
  需要网页登录态的 `userToken`（凭据名默认 `DEEPSEEK_PLATFORM_TOKEN`），**未配置则不发任何请求**
- **余额推算**：只用公开 API Key —— 每次刷新记录余额采样，**余额下降记当日消费、上升记为充值不冲抵**；
  面板显示「余额推算 ¥Z（N 次采样）」，跨 >30 分钟空档标「含空档，仅供参考」。
  历史写在 `$DSH_HOME/token-meter-panel/balance-history.json`（只留 60 天）
- **官方账单页 ↗**：面板上直接跳 `platform.deepseek.com/usage` 自己核对

- **修正高峰时段计费**：设置 schema 会把缺失的费率档整个补出来，原先两档共用同一个
  带默认值的 schema，导致 **peak 被填成空闲价（0.02/1/4）→ 高峰时段的费用少算一半**。
  现在两档各有正确的默认值，并加了回归测试 `tools/verify-rates.mjs`（纳入 `npm test`）
- **峰谷价写进面板**：「计费明细 ▾」展开后列出高峰/空闲两档、各模型的单价
- **时间范围选项移到趋势卡上**：近 7 天 / 近 30 天 / **自定义起止日期** 就在「每日消费趋势」标题旁，
  不用再跨屏点击；自定义区间按所选日期逐日画柱
- **左上角新增状态行**：标题下方显示「更新于 HH:MM:SS · 账户余额 ¥x」
  （「未开启」= 设置里关了余额；「查询失败」= 凭据缺失或接口异常）
- **余额查询改为默认开启**（原「默认零出网」策略取消；设置里关掉即完全零出网）
- **新增招牌指标**：今日消费旁边直接显示「缓存今天省下 ¥X」——
  这是本插件最该被看见、也最反直觉的一条（不再显示「若无缓存需 ¥Y」）
- **计费明细改为可折叠**：主区只留两个大数，长公式（三类单价 × 用量）收进「计费明细 ▾」
- **趋势图不再像坏了**：无数据的日子显示灰色日期（原来整行日期是透明的）、加基线，
  有数据天数 < 3 时给一句提示并可一键切到近 30 天
- **分时图加网格线**：50% / 100% 虚线参考线，看得出量级
- **预算条加粗到 8px**，并画出告警线刻度（`alertAtPercent`，默认 80%），带宽度过渡动画
- **环比可读**：`↑32% vs 上次使用` 补上对比日期（如 `· 09-19`）
- **会话明细默认只列 5 行**，超出折叠为「查看全部 (N)」，首屏留给今日/省下/趋势

## 0.1.0 — 首次发布

### 面板

- 侧栏独立视图（`sidebar.panellist` 图标 + `main` keyed 面板，id 同名）
- 今日消费：人民币金额、预算进度条、与上次使用对比、账户余额（可选）
- 四宫格指标：未命中输入 / 缓存命中输入 / 输出 token、缓存命中率 + 调用次数
- 每日趋势：近 7 天 / 近 30 天切换，按三类 token 堆叠，悬浮显示当天明细
- 今日分时：24 小时 token 分布 + 峰值小时
- 成本构成：三类 token 各自花费与占比，并给出「无硬盘缓存时的对照花费」
- 按会话明细：今日每个会话的消费降序表，子代理会话打标
- 本会话实时：来自官方 `tokenUsage` 投影的当前对话用量
- 深色 / 浅色主题自适应（全部使用 DSH 的 `--dsw-*` 主题变量）

### 采集

- 多帧 zstd 逐帧解码（`zstdDecompressSync` 只解第一帧、流式解码报 `Unknown frame descriptor`，
  两者都不可用；按帧边界扫描后逐帧解压）
- 会话日志三层目录扫描，兼容工作区下直接放日志的形态
- 同一会话 v3 与 v0 并存时只认 v3（避免用量翻倍）
- 从 `assistant/attempt` 的流式末块补回重试/中断的用量
- 按北京时间（UTC+8）分桶，与机器时区解耦
- 子代理会话归因到父会话（读 `session.parentSession` 与 `subagent/descriptor.label`）

### 计费

- DeepSeek 官方费率：`deepseek-flash` 与 `deepseek-v4-pro`，各含高峰 / 空闲两档
- 高峰判定：北京时间周一至周五 9:00–12:00、14:00–18:00（法定节假日不识别）
- 未知模型不猜价：只累加 token，不计金额
- 费率、预算、告警阈值、刷新间隔均可在设置里改，改动即时生效

### 设置

- 注册 `token-meter-panel` 设置 namespace（schemastery schema，含中文字段描述）
- 服务缺失时优雅降级：不注册设置项，其余功能照常
- `showBalance` 默认关闭：不开开关时插件零出网

### HTTP 路由（宿主半侧）

- `GET /token-meter-panel/summary` — 聚合结果，`?refresh=1` 强制重算
- `GET /token-meter-panel/balance` — 官方账户余额（可选，需凭据）
- `GET /token-meter-panel/health` — 自检
- 三条路由均复用 DSH `connection.requestRejection` 做 Host/Origin 围栏与浏览器认证
- 聚合结果带 5 秒 TTL 缓存，避免面板轮询触发全量重扫

### 已知限制

- 会话标题生成与联网搜索两次调用不落 usage，无法计入花费
- 活跃会话仍在追加写入，「今日」为下界（面板底部如实标注）
- 法定节假日不识别，节假日会被按高峰价计（偏保守）
